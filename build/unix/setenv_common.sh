#!/bin/bash

rm -rf ${BUILD_CONFIG}
mkdir ${BUILD_CONFIG}

cd ${BUILD_CONFIG}

cmake -G"${CMAKE_GENERATOR}" -DCMAKE_INSTALL_PREFIX=${INSTALL_PATH} -DCMAKE_BUILD_TYPE=${BUILD_CONFIG} -DPROJECT_ROOT=${PROJECT_ROOT} ${SOURCE_PATH}

cd ..
